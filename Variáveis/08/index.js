
function calcularComprimentoCirculo(comprimentoCirculo) {
    return 2 * Math.PI * comprimentoCirculo;
  }
  console.log(`O comprimento do círculo ${calcularComprimentoCirculo(1)}`);
 
  function calcularAreaCirculo(areaCirculo) {
    return 2 * Math.PI * areaCirculo / 2 ;
  }
  console.log(`A área do círculo ${calcularAreaCirculo(1)}`);  